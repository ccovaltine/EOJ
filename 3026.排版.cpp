#include <bits/stdc++.h>
using namespace std;

int main(){
    int T; scanf("%d\n",&T);
    for(int cas=0;cas<T;cas++){
        int M; scanf("%d\n",&M);
        char line[2008]; gets(line);
        char words[100][(M+1)/2];
        for(int i=0;line)
    }
    return 0;
}
